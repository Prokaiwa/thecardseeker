---
context: default
phase: codebase-merge
task: null
total_tasks: null
status: paused
last_updated: 2026-04-09T09:12:49.992Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stale .next/types cache | typedRoutes was disabled in next.config.js but .next/types/ still enforced RouteImpl constraints. Required `rm -rf .next/types` to clear. | advisory | Always `rm -rf .next/types` after toggling typedRoutes |
| deck-commander-main/ in tsconfig | The Vite project has its own node_modules with different @types/react and csstype versions, causing type conflicts with the Next.js root. | advisory | Ensure `deck-commander-main` stays in tsconfig `exclude` array |

<current_state>
Cold-start merge session completed. We started with two codebases:
1. `~/Desktop/the-card-seeker/` — Next.js 14 App Router foundation with design system + Esubi mascot
2. `~/Desktop/the-card-seeker/deck-commander-main/` — Lovable/Vite reference app with page structures

All core pages have been converted to Next.js App Router and restyled with our Ken Sugimori design system. TypeScript compiles clean (0 errors). Dev server runs at http://localhost:3000.

The project has no git repo yet — all files are untracked.
</current_state>

<completed_work>

- CLAUDE.md created at project root with design rules, colors, commands, quality checklist
- `tsconfig.json` — excluded `deck-commander-main/` to prevent Vite type conflicts
- `next.config.js` — disabled `typedRoutes`, added `assets.tcgdex.net` to image remotePatterns
- `src/types/index.ts` — removed `export * from './api'` to fix DeckCard/DeckListItem re-export conflict
- `src/components/layout/Navbar.tsx` — sticky shared nav, active route highlight, mobile menu
- `src/components/cards/CardTile.tsx` — card grid tile with TCGdex image, gold hover, add overlay
- `src/components/cards/CardDetailModal.tsx` — full card detail: attacks, abilities, weakness, pricing
- `src/app/cards/page.tsx` — live TCGdex card search, type/category filters, Esubi reactions
- `src/app/deck-builder/page.tsx` — two-panel builder: search panel + deck list, export to .txt
- `src/app/meta/page.tsx` — Limitless tournament meta, tier badges (S/A/B/C), format toggle
- `src/app/decks/page.tsx` — deck library with auth placeholder
- `src/app/dashboard/page.tsx` — dashboard with quick-action cards
- `src/app/ai-generator/page.tsx` — coming soon placeholder with feature list
- `src/app/page.tsx` — replaced inline nav with shared Navbar component
</completed_work>

<remaining_work>

From the original handoff plan:
- `/cards/[id]/page.tsx` — dynamic card detail page (currently only modal exists)
- `/decks/[id]/page.tsx` — dynamic deck detail page
- Supabase auth: login/signup flow, protected routes
- Deck saving to Supabase (currently in-memory only in deck-builder)
- shadcn/ui components from deck-commander-main not yet installed in Next.js project
- `/ai-generator` — actual AI integration (currently placeholder)
- Real deck list on `/decks` (requires auth)
- User collection tracking on `/dashboard`
</remaining_work>

<decisions_made>

- **Disabled typedRoutes**: Next.js typed routes enforcement breaks Link hrefs for pages that don't exist yet (/signup, /about, /privacy etc). Disabled in next.config.js and cleared .next/types cache.
- **deck-commander-main stays as reference only**: Not deleted, excluded from tsconfig. Use as reference for page structure/logic when building remaining pages.
- **No shadcn/ui installed yet**: The Lovable project had 50+ shadcn components. Rather than bulk-install, will add them as needed per page. Current pages use plain Tailwind.
- **Deck builder is in-memory**: Deck state lives in React state. Supabase persistence is a future step requiring auth.
- **src/types/index.ts**: Removed `export * from './api'` — DeckCard and DeckListItem in api.ts conflict with deck.ts. Neither type is used by any new code.
</decisions_made>

<blockers>
None blocking. Two human actions available but not required to continue:
1. Test live app: `npm run dev` → http://localhost:3000/cards
2. Supabase setup (needed for auth/deck saving): add NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY to `.env.local`
</blockers>

## Required Reading (in order)
1. `CLAUDE.md` — Design rules, color palette, quality checklist. Must be followed for every component.
2. `src/lib/ingestion/index.ts` — All API adapter exports. Use these for data fetching.
3. `src/components/esubi/Esubi.tsx` — useEsubi() hook API: `search()`, `celebrate()`, `confused()`, `error()`, `reset()`.
4. `deck-commander-main/src/pages/` — Reference only. Lovable page structures to guide remaining page conversions.

## Infrastructure State
- **Dev server**: Was running at http://localhost:3000 at pause time (background process). Run `npm run dev` to restart.
- **No git repo**: Project is entirely untracked. Consider `git init` before next session.
- **deck-commander-main/**: Vite reference project. Has its own node_modules. Do NOT `npm install` from project root expecting it to affect deck-commander-main.
- **TCGdex API**: Free, no API key. Rate limited to 3 req/s in the adapter.
- **Limitless TCG API**: Free, no API key. Tournament data — may return empty for some formats.

<context>
This was an initial merge/build session converting the Lovable prototype into our Next.js design system. The architecture is clean: shared Navbar, design-system-styled components, real API data. The foundation is solid.

The next natural step is `/cards/[id]` dynamic route (card detail page, not just modal) and then wiring Supabase auth so deck saving works. After auth, the deck-builder's in-memory state can be persisted.

The Esubi mascot is already wired globally via EsubiProvider in layout.tsx — all new pages just call useEsubi() and trigger expressions.
</context>

<next_action>
Start with: Run `npm run dev`, open http://localhost:3000/cards, search "Charizard" to verify live TCGdex data loads. Then build `/cards/[id]/page.tsx` as a standalone card detail page.
</next_action>
