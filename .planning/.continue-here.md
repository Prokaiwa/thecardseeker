---
context: default
phase: card-browser-polish-2
task: 14
total_tasks: 14
status: paused
last_updated: 2026-04-10T05:57:42.143Z
---

# BLOCKING CONSTRAINTS

_No blocking constraints discovered this session._

<current_state>
Card browser polish session 2 complete. All UI improvements done. Build is clean (0 TS errors).
Main remaining work: Supabase auth, deck saving, Vercel deploy.
</current_state>

<completed_work>
- Real PNG energy icons in /public/icons/energy/ (11 files, user-provided official symbols)
- EnergyIcon component renders PNGs directly — no CSS circle wrapper
- Featured set uses pokemontcg.io release dates to sort TCGdex sets (Perfect Order now correctly featured)
- SKIP_FEATURED regex excludes promo/energy sets from featured slot
- ERA_ORDER expanded: me/ecard/neo/gym/si/lc/base — filter dropdown now correctly ordered
- "pokemon" auto-normalizes to "Pokémon" in search
- Trainer/Energy modal: effect box always shown, fallback text if TCGdex missing data
- Modal abilities: Shield icon removed, type label (Ability/Poké-Power) now red
- Modal attacks: Swords icon removed
- Modal Weakness/Resistance/Retreat: always shown for Pokémon, shows — when empty
- Modal info grid (ALL cards): Artist, Rarity, Language, Number (e.g. 4/102), Expansion, Series
- getSeriesName() helper derives series from set ID prefix
- Jump to top button at bottom of card list
- Reset to featured set button after any search
- pokemontcg.io supplement: TCGdex missing weakness/resistance → silently fetch from ptcgio on card open
</completed_work>

<remaining_work>
- Task 15: Supabase auth (signup/login, middleware, protect /decks + /dashboard)
- Task 16: Wire Save Deck in /deck-builder (requires auth)
- Task 17: Deploy to Vercel + env vars
- Task 18: Deck detail page /decks/[id]
- Task 19: Investigate search sort edge cases (Base Set cards in Charizard search)
</remaining_work>

<decisions_made>
- pokemontcg.io secondary source only — TCGdex remains primary
- Weakness supplement fires only when BOTH weaknesses AND resistances are null (avoids extra calls)
- Series derived from set ID prefix — TCGdex card detail doesn't return serie object
- EnergyIcon renders PNG directly; user's images already have colored background baked in
</decisions_made>

<blockers>
- BLOCKING: Supabase project not yet created. Needs NEXT_PUBLIC_SUPABASE_URL + NEXT_PUBLIC_SUPABASE_ANON_KEY. supabase/schema.sql is ready.
- NON-BLOCKING: Vercel deployment not yet done.
</blockers>

## Infrastructure State
- Build: clean, 0 TypeScript errors
- Energy icons: /public/icons/energy/*.png (11 files)
- Auth: not implemented — supabase/schema.sql exists and ready

<context>
pokemontcg.io now acts as a dual supplement: (1) release dates for set ordering on mount, (2) weakness/resistance/retreat on card detail open. Both are silent on failure. Auth is the next major milestone.
</context>

<next_action>
Ask user: "Do you have a Supabase project set up yet?" If yes → get env vars → implement auth end-to-end. If no → guide through Supabase project creation first.
</next_action>
